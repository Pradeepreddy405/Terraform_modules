Module 2: Terraform Configuration Basics
Goal

Understand Terraform syntax, how to declare resources, use variables, and output resource information.

## 1. Terraform Configuration Files
.tf files → Main Terraform configuration (resources, providers, variables, outputs).
.tfvars files → Optional file to assign values to variables. Keeps sensitive or changing data separate.
Do not: hardcode values in .tf files for things that may change.

## 2. Providers and Resources

Provider → Defines the infrastructure platform (e.g., AWS).
	```
	provider "aws" {
	region = var.region
	}
	
	```
Resource → Represents an actual piece of infrastructure.
	```
	resource "aws_instance" "web" {
	ami           = var.ami_id
	instance_type = var.instance_type
	}
	```
## 3. Variables

Declared in Terraform and can be passed via .tfvars or CLI.

Types:

string → "t2.micro"

number → 1

list → ["us-west-1", "us-east-1"]

map → { name="dev" env="prod" }

Example:

variable "instance_type" {
  type    = string
  default = "t2.micro"
}
## 4. Outputs

Extract and display resource info after terraform apply.

Useful for dependent modules or just for reference.

output "instance_id" {
  value = aws_instance.web.id
}

output "public_ip" {
  value = aws_instance.web.public_ip
}
## 5. Resource Dependencies

Terraform automatically detects dependencies based on references.

Example: If a subnet is needed for an EC2 instance, Terraform ensures the subnet is created first:

resource "aws_subnet" "my_subnet" { ... }
resource "aws_instance" "web" {
  subnet_id = aws_subnet.my_subnet.id
}



## 6. Hands-On Lab

Goal: Launch an EC2 instance using variables and output key info.

### Step 1: Define variables (variables.tf)

variable "region" {}
variable "ami_id" {}
variable "instance_type" {}

### Step 2: Provider and resource (main.tf)

provider "aws" {
  region = var.region
}

resource "aws_instance" "web" {
  ami           = var.ami_id
  instance_type = var.instance_type
}

### Step 3: Create terraform.tfvars with values

region        = "us-west-2"
ami_id        = "ami-0c55b159cbfafe1f0"
instance_type = "t2.micro"

### Step 4: Outputs (outputs.tf)

output "instance_id" {
  value = aws_instance.web.id
}

output "public_ip" {
  value = aws_instance.web.public_ip
}

### Step 5: Execute commands

terraform init        # initialize modules and provider
terraform plan        # preview changes
terraform apply       # create resources
terraform destroy     # clean up


Key Notes

- Keep variables flexible; never hardcode AMIs or regions in .tf.
- Always check terraform plan before apply—Terraform will do exactly what you tell it, no more, no less.
- Use outputs for integration with other modules or automation scripts.